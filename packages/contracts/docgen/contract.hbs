{{{natspec.devdoc}}}

{{#if ownFunctions}}
    # Functions:
    {{#ownFunctions}}
        {{#if (or (eq visibility "public") (eq visibility "external"))}}
            - [`{{name}}({{args}})`](#{{anchor}})
        {{/if}}
    {{/ownFunctions}}
{{/if}}

{{#if ownEvents}}
    # Events:
    {{#ownEvents}}
        - [`{{name}}({{args}})`](#{{anchor}})
    {{/ownEvents}}
{{/if}}

{{#ownFunctions}}
    {{#if (or (eq visibility "public") (eq visibility "external"))}}
        # Function `{{name}}({{args}}){{#if outputs}} → {{outputs}}{{/if}}` {#{{anchor~}} }
        {{#if natspec.devdoc}}{{natspec.devdoc}}{{else}}Missing `devdoc` for function `{{name}}`{{/if}}
        {{#if natspec.params}}
            ## Parameters:
            {{#natspec.params}}
                - `{{param}}`: {{description}}
            {{/natspec.params}}
        {{else}}Missing `params` for function `{{name}}`{{/if}}
        {{#if natspec.returns}}
            ## Return Values:
            {{#natspec.returns}}
                - {{param}} {{description}}
            {{/natspec.returns}}
        {{else}}Missing `returns` for function `{{name}}`{{/if}}
    {{/if}}
{{/ownFunctions}}

{{#ownEvents}}
    # Event `{{name}}({{args}})` {#{{anchor~}} }
    {{#if natspec.devdoc}}{{natspec.devdoc}}{{else}}Missing `devdoc` for event `{{name}}`{{/if}}
    {{#if natspec.params}}
        ## Parameters:
        {{#natspec.params}}
            - `{{param}}`: {{description}}
        {{/natspec.params}}
    {{else}}Missing `params` for event `{{name}}`{{/if}}
{{/ownEvents}}